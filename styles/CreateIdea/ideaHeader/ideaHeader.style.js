import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles((theme) => ({
  container: {
    padding: "4rem 0",
    marginLeft: "7rem",
  },
  header: {
    color: "#333CFF",
  },
  body: {
    marginTop: "1rem",
    width: "35rem",
  },
}));
