import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles((theme) => ({
    container: {
        padding: '5rem',
    },
    campaignHeader: {
        // color: "#333CFF",
        marginLeft: "4.5rem",
        marginBottom: "2.6rem"
    }
}))
